# Registry port configuration
# Default port is 5000, but on macOS this conflicts with AirPlay Receiver
# Use a different port like 5001 for macOS compatibility
REGISTRY_PORT=5001

# Example for macOS users:
# REGISTRY_PORT=5001

# Example for Linux users (if port 5000 is available):
# REGISTRY_PORT=5000